package unsw.device;

import unsw.utils.Angle;

public class LaptopDevice extends Device {
    public LaptopDevice(String id, Angle position, String type) {
        super(id, position, type);
    }
}
