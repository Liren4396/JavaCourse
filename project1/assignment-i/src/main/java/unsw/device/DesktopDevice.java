package unsw.device;
import unsw.utils.Angle;

public class DesktopDevice extends Device {
    public DesktopDevice(String id, Angle position, String type) {
        super(id, position, type);

    }
}
